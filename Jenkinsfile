pipeline {
    agent any
    stages {
        stage('Tests') {
            steps {
                echo "Executing unit tests..."
                echo "Executing further tests..."
            }
        }
        stage('Build') {
            steps {
            echo "Compiling, or building directly, the code..."
            }
        }
    }
}
